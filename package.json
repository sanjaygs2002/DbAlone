{
  "name": "dbalone",
  "version": "1.0.0",
  "description": "JSON Server for e-commerce microfrontend project",
  "main": "server.js",
   "scripts": {
    "start": "cross-env PORT=5000 json-server --watch db.json"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
